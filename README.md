## A Hello world for OPEN AI API and Agents frameworks

# before to start 
Create a ```config.py``` file in the root directory of the project with the following content:
```python
GPT_API_KEY = "sk-..."
```
